import 'package:flutter/material.dart';

class WatchListWidget extends StatelessWidget {
  var list = <String>[];
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Look this"),
    );
  }
}